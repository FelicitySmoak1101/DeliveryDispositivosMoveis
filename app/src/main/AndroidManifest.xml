<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pizzaria">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pizzaria">
        <activity android:name=".QuartoSabor"></activity>
        <activity android:name=".TerceiroSabor" />
        <activity android:name=".SegundoSabor" />
        <activity android:name=".Status" />
        <activity android:name=".FormaPagamento" />
        <activity android:name=".Sabores" />
        <activity android:name=".QtdSaboresFamilia" />
        <activity android:name=".QtdSaboresMedia" />
        <activity android:name=".Tamanho" />
        <activity android:name=".Dashboard" />
        <activity android:name=".TelaInicial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>